# pnpm workspace settings
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# Required for Turbo on Windows — makes pnpm lay out node_modules in the flat
# structure Turbo expects when searching for its platform-specific binary.
node-linker=hoisted
